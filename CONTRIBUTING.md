# Contributing

Keep changes small and focused. Add or update a backend test for API behavior and run the frontend production build before opening a pull request. Use a short branch name, explain the behavior change in the pull request, and do not commit `.env` files or generated SQLite databases.